import 'package:flutter/material.dart';

class RandomScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('偶遇佳句'),
      ),
      body: Center(child: Text('偶遇佳句'),),
    );
  }
}