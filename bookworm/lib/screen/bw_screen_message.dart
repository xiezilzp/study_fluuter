import 'package:flutter/material.dart';

class MessageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        '书虫',
        style: TextStyle(fontFamily: 'bw_text_font', fontSize: 20.0),
      ),
    );
  }
}
